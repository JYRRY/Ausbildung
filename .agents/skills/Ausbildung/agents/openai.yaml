interface:
  display_name: "Ausbildung"
  short_description: "Repo-specific patterns and workflows for Ausbildung"
  default_prompt: "Use the Ausbildung repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true